# AgenticX GraphRAG 演示系统环境配置示例
# 复制此文件为 .env 并填入实际的配置值

# =============================================================================
# LLM 配置
# =============================================================================

# OpenAI API 配置
OPENAI_API_KEY=sk-your-openai-api-key-here
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-3.5-turbo

# SiliconFlow API 配置 (可选)
SILICONFLOW_API_KEY=sk-xxx
SILICONFLOW_BASE_URL=https://api.siliconflow.cn/v1

# 阿里云百炼 API 配置 (可选)
BAILIAN_API_KEY=sk-xxx
BAILIAN_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

# =============================================================================
# 图数据库配置
# =============================================================================

# Neo4j 配置
NEO4J_URI=bolt://localhost:7687
NEO4J_USERNAME=neo4j
NEO4J_PASSWORD=agenticx
NEO4J_DATABASE=neo4j

# NebulaGraph 配置 (可选)
NEBULA_HOST=127.0.0.1
NEBULA_PORT=9669
NEBULA_USERNAME=root
NEBULA_PASSWORD=nebula
NEBULA_SPACE=agenticx_graph

# =============================================================================
# 向量数据库配置
# =============================================================================

# Chroma 配置
CHROMA_HOST=localhost
CHROMA_PORT=8000
CHROMA_COLLECTION=agenticx_vectors

# Milvus 配置 (可选)
MILVUS_HOST=localhost
MILVUS_PORT=19530
MILVUS_USERNAME=
MILVUS_PASSWORD=
MILVUS_DATABASE=default
MILVUS_COLLECTION=agenticx_collection

# Qdrant 配置 (可选)
QDRANT_HOST=localhost
QDRANT_PORT=6333
QDRANT_API_KEY=
QDRANT_COLLECTION=agenticx_vectors

# =============================================================================
# 键值存储配置
# =============================================================================

# Redis 配置
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=password
REDIS_DB=0

# SQLite 配置 (可选)
SQLITE_PATH=./workspace/cache/agenticx.db

# =============================================================================
# 对象存储配置 (可选)
# =============================================================================

# S3 配置
S3_ENDPOINT_URL=
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=
S3_BUCKET_NAME=agenticx-storage
S3_REGION=us-east-1

# MinIO 配置
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=agenticx-storage
MINIO_SECURE=false

# =============================================================================
# 系统配置
# =============================================================================

# 工作空间配置
WORKSPACE_BASE_DIR=./workspace
WORKSPACE_CACHE_DIR=./workspace/cache
WORKSPACE_LOGS_DIR=./workspace/logs
WORKSPACE_EXPORTS_DIR=./workspace/exports

# 日志配置
LOG_LEVEL=INFO
LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s
LOG_MAX_SIZE=10MB
LOG_BACKUP_COUNT=5

# 性能配置
MAX_WORKERS=4
BATCH_SIZE=100
CACHE_TTL=3600
REQUEST_TIMEOUT=30

# =============================================================================
# 开发配置
# =============================================================================

# 调试模式
DEBUG=false
VERBOSE=false

# 测试配置
TEST_MODE=false
TEST_DATA_DIR=./tests/data

# 监控配置
ENABLE_METRICS=true
METRICS_PORT=8080
ENABLE_TRACING=false